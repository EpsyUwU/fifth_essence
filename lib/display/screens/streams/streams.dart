import 'package:flutter/material.dart';

class Streams extends StatelessWidget {
  const Streams({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hola son streams');
  }
}
